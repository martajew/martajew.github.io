---
import BlockRenderer from './BlockRenderer.astro'
import type { PageModel } from '../../lib/models'

interface Props {
  page: PageModel
}

const { page } = Astro.props
---

{page.getBlocks().map((block) => <BlockRenderer block={block} page={page} />)}
