---
import BlockRenderer from './BlockRenderer.astro'
import type { Context } from '../../lib/content'
import type { PageBlock } from '../../lib/models'

interface Props {
  blocks: PageBlock[]
  context: Context
}

const { blocks, context } = Astro.props
---

{blocks.map((block) => <BlockRenderer block={block} context={context} />)}
