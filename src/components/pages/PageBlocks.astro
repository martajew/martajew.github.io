---
import type { PageBlock } from "../../lib/content-types";
import BlockRenderer from "./BlockRenderer.astro";

const { blocks } = Astro.props as { blocks: PageBlock[] };
---

{
    blocks.map((block) => <BlockRenderer block={block} />)
}
