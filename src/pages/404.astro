---
import Layout from "../layouts/Layout.astro";
import PageBlocks from "../components/pages/PageBlocks.astro";
import { getPageDataBySlug } from "../lib/content";

const notFoundPage = await getPageDataBySlug("not-found");
---

<Layout title={notFoundPage.title}>
    <PageBlocks blocks={notFoundPage.blocks} />
</Layout>
